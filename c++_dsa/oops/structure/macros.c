#include<stdio.h>
#include<limits.h>
#include<math.h>
int main(){
    float x=sqrt(8);
    float y=cbrt(216);
    int max=INT_MAX;
    printf("%d\n",max);
    long long maxed=LONG_LONG_MAX;
    printf("%lld\n",maxed);
    return 0;
}